#include<stdio.h>
#include<conio.h>
#

void main ();
{
    char choice;
    do{
        clrscr();
        printf("select your choice :\n");
        printf("[i] ");
        fflush(stdin);
        choice=getche();
        switch(choice)
        {
            case '1'
        }
            }
}